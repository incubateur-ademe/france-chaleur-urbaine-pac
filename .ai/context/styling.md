# Styling

- Use React DSFR classes and components first.
- UI text is French.
- Keep project CSS in `src/styles.css` until the app needs a larger styling structure.
- Check responsive behavior when adding layout-heavy components.
